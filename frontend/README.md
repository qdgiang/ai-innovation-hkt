# frontend

Next.js dashboard on Vercel — initialized in Phase 3 (`create-next-app`), per
`ai-docs/plan.md`. Read-only views over the backend REST API + the Q&A box, in
priority order: decision log → blocker board → digest archive → Q&A.
