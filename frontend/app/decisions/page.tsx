// DSH-4, owner: A. Placeholder — B's scaffold only wires the shell/nav; the real
// decision/policy log + search+filter matrix is A's build (plan.md P5).
export default function DecisionsPage() {
  return (
    <div>
      <h1 className="mb-4 text-lg font-semibold">Decision log</h1>
      <p className="text-sm text-slate-500">Owner: A — DSH-4, coming in P5.</p>
    </div>
  );
}
